import React, {Component} from 'react';
import AppWrapper from "../../../app-html/AppWrapper";

export default class AddUser extends Component {
  render() {
    return(
      <AppWrapper>Add user</AppWrapper>
    )
  }
}